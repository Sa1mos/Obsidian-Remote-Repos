[Ссылка на урок](https://greg-mentor.yonote.ru/share/be10e133-ab85-4053-8c7c-03363d76447b)
## Questions

1. Почему кластерный индекс один?
2. Документо-ориентированные БД реляционные или нет?
3. Для чего используется `WHERE`?
4. Какие виды `JOIN` знаете?
5. Как настроить связи в иерархической структуре?
6. Какие типы индексов знаете?
7. Какие альтернативы подзапросам в SQL?
8. Что такое уровни изоляции транзакций?
9. Всегда ли используется индекс при поиске по индексированному столбцу?
10. Что такое индексы в базах данных? Плюсы и минусы
11. Например мы проектируем с нуля базу данных. Какую нормальную форму нам надо выбрать в каких случаях для этой базы.
12. Зачем нужен `EXPLAIN`?
13. На какие столбцы лучше добавлять индексы?
14. Знаете ли уровни изоляции транзакций?
15. Зачем нужна нормализация?
16. Какие бывают вообще отношения между таблицами?
17. Как оптимизировали хранимые процедуры?
18. Для чего используется `UNION`?
19. Что такое транзакции в БД?
20. Почему это плохое решение создавать индексы на все колонки?
21. Что такое уникальные индексы?
22. Что хранится на листовом уровне некластерного индекса на таблице с кластерным индексом?
23. Что такое селективность индекса?
24. Какие индексы бывают?
25. Что такое EAV модель?
## Answers

1. Класстерный индекс - это тип индекса, который упорядочивает таблицу на диске.
   Он один, тк данные могут быть отсортированны только одним способом.
2. Не являются.
   В реляционных БД используется система связей с помощью ключей, а в документо-ориентированных - вложенные структуры.
   Хренение данных в реляционных БД реализована в виде блоков фиксированного размера в памяти, когда как документо-ориентированные БД хранят их в виде автономных документов JSON, BSON или XML.
3. `WHERE` используется для фильтрации. Он указывает, что действие должно применяться только к тем записям, которые соответсвуют заданному условию.
4. `INNER JOIN` - Только совпадающие строки из обеих таблиц
   `LEFT JOIN` - Все строки из левой + совпадения из правой (NULL если нет)
   `RIGHT JOIN` - Все строки из правой + совпадения из левой (NULL если нет)
   `FULL OUTER JOIN` - Все строки из обеих таблиц, NULL там где нет совпадений
   `CROSS JOIN` - Декартово произведение — каждая строка с каждой
   `NATURAL JOIN` - Автоматически соединяет по одноимённым столбцам
   `SELF JOIN` - Таблица соединяется сама с собой (через алиас)
   `LATERAL JOIN` - Подзапрос справа может ссылаться на столбцы из левой части
5. Иерархическая структура — это способ организации данных в виде **дерева**, где каждый элемент (узел) имеет ровно одного родителя, а корневой элемент — не имеет ни одного.
   Виды связей:
	  - Adjacency List (Список смежности)
		Самый простой способ — каждая запись хранит ссылку на родителя.
	  - Materialized Path (Путь в строке)
	    Каждая запись хранит полный путь от корня.
	  - Nested Sets (Вложенные множества)
	    Каждый узел хранит левую и правую границу своего поддерева
	  - Closure Table (Таблица замыканий)
	    Отдельная таблица хранит **все** пары предок–потомок.
6. Виды индексов:
	   - B-Tree (по умолчанию)
	     Самый распространённый. Подходит для большинства задач.
	   - Hash
	     Оптимизирован только для проверки равенства.
	   - GiST (Generalized Search Tree)
	     Обобщённое дерево поиска — фреймворк для сложных типов данных.
	   - GIN (Generalized Inverted Index)
	     Инвертированный индекс — один ключ указывает на множество строк.
	   - BRIN (Block Range Index)
	     Хранит мин/макс значения для диапазонов блоков — очень компактный.
	   - SP-GiST (Space-Partitioned GiST)
	     Разбивает пространство на непересекающиеся области.
7. Альтернативы подзапросам `JOIN`, `CTE (WITH)`, `LATERAL JOIN`, оконные функции (`OVER`).
8. Настройка, что определяет, насколько сильно параллельные транзакции "Видят" друг друга. Чем выше уровень изоляции — тем меньше аномалий, но больше блокировок.
9. Нет. Планировщик может игнорировать индекс если: выборка > ~10–15% таблицы, низкая селективность, устаревшая статистика, или таблица слишком мала.
10. Индексы ускоряют чтение, сортировку и поиск. Замедляют `INSERT/UPDATE/DELETE`.
11. 3НФ — стандарт для большинства OLTP-систем (бизнес-логика, транзакции).
    BCNF — когда 3НФ недостаточно строга.
    Денормализация (1–2НФ) — OLAP, аналитика, хранилища данных (скорость чтения важнее)
12. `EXPLAIN` показывает план выполнения запроса: какие индексы используются, стоимость, порядок JOIN. `EXPLAIN ANALYZE` — ещё и реальное время выполнения.
13. Часто используемые в `WHERE`, `JOIN ON`, `ORDER BY`, `GROUP BY`. Приоритет — высокоселективные столбцы (много уникальных значений).
14. READ UNCOMMITTED, READ COMMITED, REPETABLE READ, SERIALIZABLE
15. Нормализация устраняет дублирование данных, аномалии вставки/обновления/удаления, обеспечивает целостность.
16. `One-to-One`, `One-to-Many`, `Many-to-Many` (через промежуточную таблицу).
17. 